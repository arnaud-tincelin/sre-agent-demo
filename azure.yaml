name: sre-agent-demo
infra:
  provider: bicep
  path: infra

services:
  zava-web:
    project: src/web
    host: containerapp
    language: python

hooks:
  postprovision:
    shell: sh
    run: ./scripts/post-provision.sh
